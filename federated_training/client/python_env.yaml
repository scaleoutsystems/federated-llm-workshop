name: .venv_client
build_dependencies:
  - pip
  - setuptools
  - wheel
dependencies:
  - scaleout
  - transformers==4.52.4
  - datasets
  - peft==0.15.2
  - trl==0.12.2
  - accelerate          # required for device_map="auto" in QLoRA path
  - bitsandbytes>=0.43.0  # multi-backend build; QLoRA active only when CUDA is present